import React, { ReactElement } from 'react'

interface Props {
    
}

function Isolate({}: Props): ReactElement {
    return (
        <div>
            Isolate
        </div>
    )
}

export default Isolate
